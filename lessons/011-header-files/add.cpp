#include "add.h"
// #include here is not a must, but it is a good practice to include the paired header file here.
// This allows to catch certain errors at compile time instead of at link time.

int add(int x, int y)
{
    return x + y;
}